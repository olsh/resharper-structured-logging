using System;

using Microsoft.Extensions.Logging;

namespace ConsoleApp
{
    public static class LogMessages
    {
        private static readonly Action<ILogger, string, Exception> HostChecked =
            LoggerMessage.Define<string>(LogLevel.Information, new EventId(4), "Host {Host}", new LogDefineOptions());
    }
}

---------------------------------------------------------
