using Microsoft.Extensions.Logging;
using ZLogger;

namespace ConsoleApp
{
    public static partial class LogMessages
    {
        [ZLoggerMessage(EventId = 0, Level = LogLevel.Information, Message = |"Could not open socket to {HostName}."|(0))]
        public static partial void CouldNotOpenSocket(ILogger logger, string hostName);
    }
}

---------------------------------------------------------
(0): ReSharper Warning: Log event messages should be fragments, not sentences. Avoid a trailing period/full stop.
