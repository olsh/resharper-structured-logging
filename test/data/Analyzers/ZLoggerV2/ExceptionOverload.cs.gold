using System;

using Microsoft.Extensions.Logging;
using ZLogger;

namespace ConsoleApp
{
    class A
    {
        public A(ILogger<A> log, Exception exception, string host)
        {
            log.ZLogError(exception, $"Could not open socket to {host:@Host}");
        }
    }
}

---------------------------------------------------------
