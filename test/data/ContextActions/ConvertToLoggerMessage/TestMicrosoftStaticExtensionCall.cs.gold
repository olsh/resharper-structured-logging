using Microsoft.Extensions.Logging;

namespace ConsoleApp
{
    public static class Program
    {
        public static void Main(ILogger logger, string host)
        {
            Log.{spotstart#1}{caret}Connecting{spotend#1}(logger, host);
        }
    }

    internal static partial class Log
    {
      [LoggerMessage(Level = LogLevel.Trace, Message = "Connecting to {Host}")]
      public static partial void Connecting(ILogger logger, string host);
    }
}

LOOKUP ITEMS IN HOTSPOTS:
  spot#1 [Count: 1, Focus: HardAtStart] Connecting
