using System;
using Microsoft.Extensions.Logging;

namespace ConsoleApp
{
    public static class Program
    {
        public static void Main(ILogger logger, Exception exception, int orderId)
        {
            {caret}Log.{spotstart#1}OrderFailedShip{spotend#1}(logger, exception, orderId);
        }
    }

    internal static partial class Log
    {
      [LoggerMessage(Level = LogLevel.Error, Message = "Order {OrderId} failed to ship")]
      public static partial void OrderFailedShip(ILogger logger, Exception exception, int orderId);
    }
}

LOOKUP ITEMS IN HOTSPOTS:
  spot#1 [Count: 1, Focus: HardAtStart] OrderFailedShip
