using Microsoft.Extensions.Logging;

namespace ConsoleApp
{
    public static class Program
    {
        public static void Main(ILogger logger, string userName)
        {
            {caret}Log.{spotstart#1}UserWasThrottled{spotend#1}(logger, userName);
        }
    }

    internal static partial class Log
    {
      [LoggerMessage(EventId = 42, Level = LogLevel.Warning, Message = "User {UserName} was throttled")]
      public static partial void UserWasThrottled(ILogger logger, string userName);
    }
}

LOOKUP ITEMS IN HOTSPOTS:
  spot#1 [Count: 1, Focus: HardAtStart] UserWasThrottled
