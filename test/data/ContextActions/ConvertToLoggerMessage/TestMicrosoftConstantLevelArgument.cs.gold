using Microsoft.Extensions.Logging;

namespace ConsoleApp
{
    public static class Program
    {
        public static void Main(ILogger logger, int retries)
        {
            {caret}Log.{spotstart#1}RetriedTimes{spotend#1}(logger, retries);
        }
    }

    internal static partial class Log
    {
      [LoggerMessage(Level = LogLevel.Critical, Message = "Retried {Retries} times")]
      public static partial void RetriedTimes(ILogger logger, int retries);
    }
}

LOOKUP ITEMS IN HOTSPOTS:
  spot#1 [Count: 1, Focus: HardAtStart] RetriedTimes
