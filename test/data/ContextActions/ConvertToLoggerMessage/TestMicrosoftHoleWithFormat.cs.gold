using Microsoft.Extensions.Logging;

namespace ConsoleApp
{
    public static class Program
    {
        public static void Main(ILogger logger, decimal amount)
        {
            {caret}Log.{spotstart#1}PaymentCaptured{spotend#1}(logger, amount);
        }
    }

    internal static partial class Log
    {
      [LoggerMessage(Level = LogLevel.Information, Message = "Payment captured for {Amount,10:000}")]
      public static partial void PaymentCaptured(ILogger logger, decimal amount);
    }
}

LOOKUP ITEMS IN HOTSPOTS:
  spot#1 [Count: 1, Focus: HardAtStart] PaymentCaptured
