using Microsoft.Extensions.Logging;

namespace ConsoleApp
{
    public static class Program
    {
        public static void Main(ILogger logger, int orderId, string country)
        {
            {caret}Log.{spotstart#1}OrderShipped{spotend#1}(logger, orderId, country);
        }
    }

    internal static partial class Log
    {
      [LoggerMessage(Level = LogLevel.Information, Message = "Order {OrderId} shipped to {Country}")]
      public static partial void OrderShipped(ILogger logger, int orderId, string country);
    }
}

LOOKUP ITEMS IN HOTSPOTS:
  spot#1 [Count: 1, Focus: HardAtStart] OrderShipped
