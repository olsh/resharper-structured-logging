using Microsoft.Extensions.Logging;

namespace ConsoleApp
{
    public static class Program
    {
        public static void Main(ILogger logger, LogLevel level, int retries)
        {
            {caret}Log.{spotstart#1}RetriedTimes{spotend#1}(logger, level, retries);
        }
    }

    internal static partial class Log
    {
      [LoggerMessage(Message = "Retried {Retries} times")]
      public static partial void RetriedTimes(ILogger logger, LogLevel level, int retries);
    }
}

LOOKUP ITEMS IN HOTSPOTS:
  spot#1 [Count: 1, Focus: HardAtStart] RetriedTimes
