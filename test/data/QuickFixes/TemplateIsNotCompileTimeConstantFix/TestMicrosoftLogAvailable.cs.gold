using Microsoft.Extensions.Logging;

namespace ConsoleApp
{
    public static class Program
    {
        public static void Main(ILogger logger, string host)
        {
            logger.LogInformation(|$"Could not open socket to {host}"|(0));
        }
    }
}

------------------------------------------------
0: Message template should be compile time constant
QUICKFIXES:
Convert to compile-time constant message template
